#include <stdio.h>

int main(int argc, char const *argv[])
{
    /* code */
    int total=10,i=90;
    total+=i;
    //this is same as total+i
    printf("total=%d\n",total);
    return 0;
}
